# 이벤트 기반 STORY 발행 — 상세설계

상태 **설계 완료 · 착수 보류** · 착수 예정 2026-09-27 (관찰 종료 후)
작성 2026-09-13 · 대상 `ryuryan091-spec/threads-promo`

---

## 0. 요약

EDT 에피소드 트래커에 새 회차가 올라오면, 그 회차를 근거로 Threads에 STORY 글을 발행한다.

| 항목 | 내용 |
|---|---|
| 트리거 | Notion 트래커 신규 행 (`created_time` 시간창) |
| 발행 기둥 | STORY (정기 로테이션에서 제거) |
| 상태 저장 | 없음 (시간창 + Threads 조회로 대체) |
| 추가 비용 | 0원 (Actions 대부분 즉시 종료) |
| **선결 항목** | 인덱스 축 확장 (2-5) — **2026-09-13 구현 완료** |

---

## 1. 착수를 보류하는 이유

발행 패턴을 바꾸므로 2주 관찰(09-14 ~ 09-27)의 데이터가 오염된다. 관찰의 목적은 기둥별 답글 유발력 비교인데, 발행 빈도와 기둥 비율이 중간에 바뀌면 비교가 불가능해진다.

워치독은 감시만 하고 발행에 개입하지 않으므로 먼저 착수했다.

---

## 2. 핵심 설계 판단

### 2-1. "새로운 내용"을 무상태로 판정하는 방법

"새로운"은 본질적으로 이전 상태와의 비교다. 무상태 원칙과 충돌한다.

| 방법 | 상태 필요 | 채택 |
|---|---|---|
| 마지막 처리 회차 번호 저장 | 필요 | ✕ 원칙 위반 |
| Threads 본문에서 회차 식별 | 불필요 | ✕ 회차 번호·캐릭터명을 쓰지 않기로 함 |
| **`created_time` 시간창 조회** | **불필요** | **✓** |

워크플로우 주기와 조회 시간창을 일치시킨다. 6시간마다 돌면서 `created_time > now - 6h`를 조회하면, 상태 없이 "지난 6시간 내 신규"를 판정할 수 있다.

```
Notion query filter
  timestamp = created_time
  created_time.on_or_after = (now - WINDOW_HOURS)
  properties: 발행 상태 = 완료
```

### 2-2. 시간창 방식의 한계

| 위험 | 내용 | 완화 |
|---|---|---|
| 중복 발행 | 수동 재실행 시 같은 창을 재처리 | Threads 최근 글 시각 확인 후 스킵 |
| 경계 누락 | 창 경계에 걸친 행 | 창을 주기보다 20% 넓게 (6h 주기 → 7.2h 창) |
| 경계 중복 | 창을 넓힌 대가 | 위 스킵 규칙이 함께 흡수 |

**완전 차단은 무상태를 유지하는 한 불가능하다.** 실용적 수준으로 낮추는 것이 목표다.

### 2-3. 기둥 재설계 — STORY를 이벤트 전용으로

**전제 갱신 (2026-09-13)**: BUILD 기둥은 이미 제거되었다. 사실성 사고 2건이 모두 BUILD 에서 발생했기 때문이다.
브리프가 "삽질과 사소한 깨달음"이라는 구체적 사건을 요구했는데, 커밋 로그는 "무엇을 했는지"만 알려주고
"어떻게 막혔는지"는 알려주지 않는다. 근거가 커버하지 못하는 영역을 요구하는 구조였다.

현재 로테이션 (8일 주기)

```python
PILLAR_ROTATION = ("STORY", "MARKET", "PROMO", "STORY",
                   "MARKET", "STORY", "PROMO", "MARKET")
# STORY 37.5% / MARKET 37.5% / PROMO 25%
```

EDT 는 주 2~3회 발행한다. 이벤트 발행을 그대로 추가하면 비율이 무너진다.

| | 현재 | 단순 추가 시 |
| --- | --- | --- |
| 주간 발행 | 7회 | 9~10회 |
| STORY 비중 | 37.5% | **약 55%** |

STORY 가 절반을 넘으면 MARKET·PROMO 가 밀린다. 특히 PROMO 가 줄면 유입 유도 자체가 약해진다.

**채택안**: 정기 로테이션에서 STORY 를 빼고 이벤트 전용으로 돌린다.

```python
# 변경 후 (6일 주기) — STORY 제거
PILLAR_ROTATION = ("MARKET", "PROMO", "MARKET",
                   "PROMO", "MARKET", "PROMO")
```

| | 정기 | 이벤트 | 합계 |
| --- | --- | --- | --- |
| MARKET | 주 3.5회 | — | 3.5 |
| PROMO | 주 3.5회 | — | 3.5 |
| STORY | 0 | 주 2~3회 | 2~3 |
| **계** | **7** | **2~3** | **9~10** |

일 1.3~1.4회. 안전 구간(일 1~2회) 안이다.

이 구조의 이점은 **"회차가 올라왔을 때만 작품 이야기를 한다"**가 자연스럽다는 것이다.
근거가 없으면 STORY 가 아예 나가지 않으므로, 근거 없는 창작 일반론을 지어낼 여지도 사라진다.

#### 🔴 부작용 — 근거 커버리지가 0이 되는 구간

정기 로테이션이 MARKET·PROMO 만 남으면 **정기 발행 전부가 근거 없는 기둥**이 된다.

| 발행 | 근거 |
| --- | --- |
| 정기 (주 7회) | 없음 |
| 이벤트 (주 2~3회) | Notion 트래커 |

BUILD 제거 이후 STORY 가 유일한 근거 기둥이었는데, 그마저 정기에서 빠진다.
사실성 위험이 정기 발행 쪽에 집중되므로, 착수 전에 다음 중 하나를 결정해야 한다.

| 선택 | 내용 |
| --- | --- |
| A | MARKET 에도 근거 소스 확보 (EDT 시장 스냅샷 DB 등) |
| B | STORY 를 정기에도 일부 남김 (예: 8일 중 1회) |
| C | 현행 유지 — 사실성 육안 점검으로 관리 |

이 결정은 7장 미해결 항목에 포함한다.

### 2-4. 안티봇 — 시각 종속 문제

이벤트 발행은 EDT 발행 시각에 종속된다. EDT가 비슷한 시각에 올라오면 Threads도 그 패턴을 따라간다.

**완화**: 감지 후 랜덤 지연을 기존보다 넓게 잡는다.

```python
ANTIBOT_EVENT_JITTER = (600, 3000)   # 10~50분
```

### 2-5. ✅ 선결 항목 — 인덱스 축 확장 (구현 완료 2026-09-13)

**이 항목을 처리하지 않고 착수하면 같은 날 동일한 이미지·기둥·소재가 두 번 나간다.**

#### 결함

현재 콘텐츠 선택은 날짜 하나만 축으로 쓴다.

```python
def _day_index(today: dt.date) -> int:
    return today.timetuple().tm_yday      # 날짜만. 실행 회차 개념이 없음
```

하루 1회 발행을 전제한 설계다. 같은 날 재실행 시 동일 결과를 보장하는 멱등성이 목적이었는데,
다회 발행에서는 그 성질이 그대로 결함이 된다.

실측(2026-09-16, 자산 5개 기준)

```
정기 발행   이미지=promo_05.png  기둥=STORY
이벤트 발행 이미지=promo_05.png  기둥=STORY
→ 충돌
```

본문 텍스트만 다르다. AI 가 매번 새로 생성하고 중복 회피 프롬프트가 걸리기 때문이다.
그러나 기둥과 소재가 같으므로 결국 비슷한 이야기가 나온다.

#### 영향

| 항목 | 결과 |
| --- | --- |
| 동일 이미지 반복 | Threads 가 반복 콘텐츠를 강등. 계정 품질 누적 하락 |
| 동일 기둥·소재 | 같은 주제가 하루 두 번 |
| 자산 소진 가속 | 자산 5개면 반복 주기가 5일에서 4일로 |

#### 해결 — 축을 하나 늘린다

```python
DISCRIMINATOR_MAX = 8

def run_index(today: dt.date, discriminator: int = 0) -> int:
    return today.timetuple().tm_yday + discriminator
```

**곱셈이 아니라 덧셈이다.** 초기 구현에서 `day * N + disc` 를 썼다가
N(8) 이 로테이션 길이(8)의 배수라 나머지가 항상 같아지는 결함이 실측으로 확인됐다.
슬롯 A 만 쓰면 8주 내내 같은 기둥만 나왔다.
덧셈은 로테이션 길이와 무관하게 안전하고, 고정 슬롯에서 하루 한 칸씩 전진하는
기존 동작도 그대로 유지된다.

`discriminator` 후보

| 방식 | 값 | 멱등성 |
| --- | --- | --- |
| 정기 슬롯 | `SLOT` 환경변수 A=0 / B=1 / C=2 | 유지 |
| 이벤트 발행 | 4 (정기와 충돌하지 않는 값) | 유지 |
| 실행 시각 | 시·분 | **깨짐. 채택 불가** |

시각 기반은 같은 슬롯 재실행 시 결과가 달라져 멱등성이 깨진다. 채택하지 않는다.

#### 적용 범위

`_day_index` 를 쓰는 모든 지점을 함께 바꿔야 한다. 하나만 바꾸면 기둥과 이미지가 어긋난다.

| 위치 | 용도 |
| --- | --- |
| `content.build_plan` | 자산 후보 정렬 시작점 |
| `ai_writer.pick_pillar` | 기둥 선택 |
| `ai_writer.pick_seed` | 소재 선택 |
| `content.pick_text` | 정적 폴백 텍스트 선택 |
| `content.pick_kind` | 홍보/관찰 구분 |

#### 현재 운영에는 무해

일 1회 발행에서는 `discriminator` 가 항상 같은 값이므로 동작이 변하지 않는다.
따라서 이벤트 발행 착수 전에 미리 반영해도 관찰 데이터에 영향이 없다.

### 2-6. 자산 수량 요건

이벤트 발행이 붙으면 주 9~10회가 된다. 자산이 적으면 반복이 눈에 띈다.

| 자산 수 | 반복 주기 (주 9~10회 기준) |
| --- | --- |
| 5개 | 4일 |
| 10개 | 1주 |
| **15~20개** | **2주. 권장 하한** |

인덱스 축을 확장해도 자산이 적으면 반복은 남는다. 두 가지를 함께 처리해야 한다.

---

## 3. 파이프라인

```
publish_story.yml  (6시간 주기: KST 02:17 / 08:17 / 14:17 / 20:17)
  │
  ├─ Notion 트래커 조회 (created_time > now - 7.2h, 발행 상태=완료)
  │    └─ 신규 없음 → 즉시 종료          ← 대부분의 실행
  │
  ├─ 안전장치 검사
  │    ├─ 직전 N시간 내 Threads 발행이 있는가 → 있으면 스킵 (중복 방지)
  │    ├─ 일일 이벤트 상한 초과했는가 → 초과 시 스킵
  │    └─ 발행 쿼터 잔여 2 이상인가
  │
  ├─ 랜덤 지연 10~50분
  │
  ├─ STORY 기둥 생성 (근거 = 그 신규 회차)
  │    └─ 캐릭터 고유명·시장 수치 차단은 기존 필터 그대로
  │
  ├─ 이미지 선택 → 검증 → 폴백 체인 (기존 로직 재사용)
  │
  └─ 발행 → 셀프 리플라이(링크)
```

---

## 4. 신규 설정

### Secrets
추가 없음. `NOTION_TOKEN` / `NOTION_DB_ID` 재사용.

### Variables

| 이름 | 기본값 | 용도 |
|---|---|---|
| `EVENT_STORY_ENABLED` | `false` | 이벤트 발행 스위치. 관찰 종료 후 `true` |
| `EVENT_WINDOW_HOURS` | `7.2` | 조회 시간창. cron 주기의 1.2배 |
| `EVENT_DAILY_CAP` | `2` | 하루 이벤트 발행 상한 |
| `EVENT_MIN_GAP_HOURS` | `4` | 직전 발행과 최소 간격 |

### 코드 변경

| 파일 | 변경 |
|---|---|
| `src/notion_source.py` | `fetch_new_episodes(since)` 추가 — 시간창 필터 |
| `src/ai_writer.py` | `PILLAR_ROTATION`에서 STORY 제거 |
| `src/run_story.py` | 신규 엔트리포인트 |
| `.github/workflows/publish_story.yml` | 신규 |
| `src/config.py` | 위 Variables, `RUNS_PER_DAY_MAX` |
| `src/content.py` · `src/ai_writer.py` | **`_day_index` → `_run_index` 전면 교체 (선결)** |

`main.py`의 발행 로직(이미지 폴백·컨테이너 대기·셀프 리플라이)은 그대로 재사용한다. 중복 구현하지 않는다.

---

## 5. 실패 처리

| 실패 지점 | 처리 |
|---|---|
| Notion 조회 실패 | 종료 (발행하지 않음). 알림 |
| 신규 없음 | 조용히 종료. 알림 없음 |
| 안전장치 차단 | 로그만. 알림 없음 |
| 생성 실패 | 재시도 2회 → 실패 시 발행 안 함 (정적 폴백 없음) |
| 이미지 실패 | 기존 폴백 체인 (Tier 2 → 텍스트 전용) |
| 발행 실패 | 알림 |

**정적 텍스트 폴백을 두지 않는 이유**: 이벤트 발행은 특정 회차를 근거로 하는 글이다. 근거 없는 정적 문구로 대체하면 이벤트의 의미가 사라진다. 차라리 안 나가는 편이 낫다.

---

## 6. Actions 분 추정

| 항목 | 계산 | 월 소모 |
|---|---|---|
| 신규 없음 (대부분) | 4회/일 × 0.5분 × 30 × 0.7 | 42분 |
| 신규 있음 | 4회/일 × 0.3 × (1분 + 지연 최대 50분) × 30 | 최대 1,800분 |

**지연이 Actions 분을 소모한다는 점이 중요하다.** Public 레포에서는 무료지만, Private 전환 시 한도(2,000분)를 위협한다.

Private 전환을 검토한다면 지연 범위를 10~20분으로 줄여야 한다.

---

## 7. 미해결 · 착수 전 결정 필요

| # | 항목 | 선택지 |
|---|---|---|
| 1 | 캐릭터 고유명 노출 | 차단(현행) / 허용 |
| 2 | 외전(EX-NN) 포함 여부 | 포함 / 본편만 |
| 3 | `발행 상태` 필터 | 완료만 / 진행중 포함 |
| 4 | 정기 STORY 완전 제거 | 제거(권고) / 축소 유지 |
| 5 | ~~`RUNS_PER_DAY_MAX` 값~~ | 완료. `DISCRIMINATOR_MAX = 8`, 이벤트 = 4 |
| 6 | **정기 근거 커버리지 0 대응 (2-3)** | A 근거 확보 / B STORY 일부 잔류 / C 현행 |

3번 주의: `진행중`을 포함하면 아직 X에 올라가지 않은 회차를 Threads가 먼저 언급하는 사고가 난다. **`완료`만 권장한다.**

---

## 8. 착수 순서

```
09-27  관찰 종료 · 데이터 해석
  ↓
1. 위 결정 4건 확정
2. ~~인덱스 축 확장 (2-5)~~ — 완료
3. **자산 15개 이상 확충 (2-6)**
4. notion_source.fetch_new_episodes 구현 + 테스트
5. PILLAR_ROTATION 변경 (STORY 제거)
6. run_story.py + publish_story.yml
7. EVENT_STORY_ENABLED=false 로 dry_run 검증
8. **같은 날 정기·이벤트 두 번 실행해 이미지·기둥이 다른지 확인**
9. true 전환
10. 1주 관찰 후 상한·지연 조정
```
