# 인사이트 수집 — 상세설계

상태 **설계 완료 · 착수 대기** · 작성 2026-09-13
대상 `ryuryan091-spec/threads-promo` · 해결 대상 요구사항 미결 U1

---

## 0. 핵심 발견 — `clicks` 메트릭이 U1을 직접 해결한다

요구사항 정의서 미결 U1은 "유입 측정 수단 부재"였다. UTM 파라미터 도입을 후속 과제로 남겼는데, **Threads 인사이트 API가 링크 클릭 수를 링크별로 직접 제공한다.**

```json
{
  "name": "clicks",
  "link_total_values": [
    { "value": 11, "link_url": "https://www.youtube.com/@..." }
  ]
}
```

공식 정의는 "사용자가 공유한 URL을 클릭한 횟수"이며, 응답이 `link_url` 단위로 쪼개져 나온다.

| 방식 | 측정 대상 | 신뢰도 |
|---|---|---|
| UTM 파라미터 | YouTube Analytics의 유입 소스 | Threads가 `l.threads.com`으로 감싸므로 파라미터 보존 여부 불확실 |
| **`clicks` 메트릭** | **Threads가 직접 센 클릭 수** | 플랫폼 1차 데이터 |

**UTM보다 직접적이고 확실하다.** UTM은 후속 과제에서 제외하고 `clicks`로 대체한다.

### 0-1. 정정 — `clicks` 는 게시물별로 나오지 않는다

초기 설계에서 기둥별 클릭 비교가 가능한 것처럼 적었으나 틀렸다.

| 메트릭 | 단위 |
| --- | --- |
| 게시물 인사이트 | 게시물별 (조회·좋아요·답글·리포스트·인용) |
| `clicks` | **사용자 단위, 링크별 집계** |

`clicks` 는 "이 URL이 몇 번 클릭됐나"만 알려준다. 어느 게시물에서 눌렀는지는 알 수 없다.

### 0-2. 우회 — 일 단위 귀속

하루 1회 발행 설계가 여기서 이득을 준다.

```
since = D, until = D  ->  그날의 클릭 수
그날의 발행은 1건      ->  그 게시물의 기둥에 귀속
```

날짜 단위로 조회하면 **날짜 → 기둥 → 클릭**이 연결된다.

**정확한 귀속은 아니다.** 과거 게시물이 계속 노출되며 발생한 클릭도 그날 집계에 섞인다. 근사치로 다뤄야 하며, 이 한계가 12장 안전장치의 근거다.

단, 이 값은 "클릭"이지 "유입"은 아니다. 클릭 후 이탈은 잡히지 않는다. YouTube 구독자 증감과 함께 봐야 한다.

---

## 1. 공식 사양

### 1-1. 권한

| 스코프 | 용도 |
|---|---|
| `threads_basic` | 모든 호출의 전제 |
| `threads_manage_insights` | 인사이트 GET |

### 1-2. 게시물 인사이트

```
GET /{threads-media-id}/insights?metric=views,likes,replies,reposts,quotes,shares
```

| 메트릭 | 설명 | 비고 |
|---|---|---|
| `views` | 재생·표시 횟수 | **개발 중 라벨** |
| `likes` | 좋아요 수 | |
| `replies` | 답글 수 | 루트 게시물이면 전체, 답글이면 직접 답글만 |
| `reposts` | 리포스트 수 | |
| `quotes` | 인용 수 | |
| `shares` | 공유 수 | **개발 중 라벨** |

제약

- 중첩 답글 메트릭은 포함되지 않는다
- `REPOST_FACADE` 게시물은 빈 배열을 반환한다

### 1-3. 사용자 인사이트

```
GET /{threads-user-id}/threads_insights?metric=...&since=...&until=...
```

| 메트릭 | 응답 형태 | 비고 |
|---|---|---|
| `views` | 시계열 | 프로필 조회 수 |
| `likes` · `replies` · `reposts` · `quotes` | 총계 | `replies`는 최상위 답글만 |
| **`clicks`** | **링크별 총계** | **U1 해결** |
| `followers_count` | 총계 | `since`/`until` 미지원 |
| `follower_demographics` | 총계 | **팔로워 100명 이상 필요**, `breakdown` 1개만 |

제약

- `since`/`until` 은 2024-04-13(Unix 1712991600) 이전 거부
- 생략 시 어제~오늘 2일 범위가 기본
- 사용자 인사이트는 2024-06-01 이전 동작이 보장되지 않는다

---

## 2. 핵심 설계 문제 — 게시물과 기둥의 연결

관찰의 목적은 **기둥별 답글 유발력 비교**다. 그런데 인사이트는 게시물 ID 단위로 나오고, **어느 게시물이 어느 기둥이었는지 기록이 없다.** 무상태 원칙(C1) 때문이다.

### 2-1. 후보 비교

| 방법 | 상태 필요 | 판정 |
|---|---|---|
| 발행 시 게시물ID↔기둥 저장 | **필요** | 무상태 위반 |
| 본문 텍스트로 역추론 | 불필요 | 부정확 |
| **발행 시각으로 슬롯 역추론** | **불필요** | **채택** |

### 2-2. 채택안 — 시각 → 슬롯 → 기둥 복원

발행 슬롯은 고정 시각이고, 지터는 최대 8분이다. 게시물 `timestamp` 로 어느 슬롯이었는지 판정할 수 있다.

```
timestamp 08:23~08:31  → 슬롯 A → discriminator 0
timestamp 12:47~12:55  → 슬롯 B → discriminator 1
timestamp 20:31~20:39  → 슬롯 C → discriminator 2
그 외                   → 이벤트 또는 수동 → discriminator 4 또는 0
```

```python
def restore_pillar(posted_at: datetime) -> str | None:
    disc = discriminator_from_timestamp(posted_at)
    if disc is None:
        return None                       # 판정 불가. 집계에서 제외
    idx = content.run_index(posted_at.date(), disc)
    return ai_writer.pick_pillar(idx)
```

**상태 저장 없이 기둥을 복원한다.** 날짜 결정론 설계(FR-03)가 여기서 이득을 준다.

### 2-3. 한계 — 명시한다

| 상황 | 결과 |
|---|---|
| 수동 실행 | `discriminator=0` 이라 슬롯 A와 구분 불가 |
| 이벤트 발행 | 슬롯 시각 밖이므로 이벤트로 판정 가능 |
| 슬롯 시각 변경 후 | 변경 이전 게시물은 오판정 |
| 판정 불가 | 집계에서 제외하고 건수를 보고 |

**오판정을 숨기지 않는다.** 리포트에 "판정 불가 N건"을 함께 표기한다.

---

## 3. 저장 전략 — 저장하지 않는다

### 3-1. 판단

인사이트는 **언제든 재조회 가능한 데이터**다. 저장할 이유가 약하다.

| 방식 | 상태 | 판정 |
|---|---|---|
| Notion DB에 일일 기록 | Notion 쓰기 권한 추가 | 과설계 |
| GitHub Secret에 누적 | 크기 제한, 부적합 | 불가 |
| **조회 후 리포트만 발송** | **없음** | **채택** |

관찰 기간은 2주다. 2주치 시계열이라면 **사람이 텔레그램 메시지를 모아 보는 것으로 충분**하다. 저장소를 만들 규모가 아니다.

### 3-2. 재평가 조건

| 조건 | 조치 |
|---|---|
| 관찰이 2개월 이상으로 길어짐 | Notion DB 기록 도입 검토 |
| 기둥이 5개 이상으로 늘어남 | 자동 집계 필요 |
| 추세 분석이 필요해짐 | 시계열 저장 |

---

## 4. 파이프라인

```
insights.yml   매일 KST 23:47 (하루 마감 후)
  ↓
사용자 인사이트 조회 (1회)
  metric=views,likes,replies,reposts,quotes,clicks,followers_count
  ↓
최근 게시물 조회 (1회)  — 지난 7일
  ↓
게시물별 인사이트 조회 (N회, 최대 7)
  metric=views,likes,replies,reposts,quotes
  ↓
timestamp → 슬롯 → 기둥 복원
  ↓
기둥별 집계 + 링크별 클릭 수
  ↓
텔레그램 리포트 발송 + Actions Summary 출력
```

### 호출 수

| 항목 | 횟수 |
|---|---|
| 사용자 인사이트 | 1 |
| 게시물 목록 | 1 |
| 게시물별 인사이트 | 최대 7 |
| **합계** | **최대 9회/일** |

기존 일일 호출 24회에 더해 33회. 한도 대비 여전히 미미하다.

---

## 5. 리포트 형식

```
[Threads 일일 리포트] 2026-09-20

팔로워 128 (+3)
프로필 조회 412

링크 클릭
  YouTube  7
  X        2

최근 7일 기둥별 (게시물 7건)
  기둥      발행  답글  좋아요  조회
  MARKET     4     2      9     310
  STORY      2     5     14     280
  PROMO      1     0      3      95
  판정불가   0

답글 최다: STORY (평균 2.5)
```

**답글을 먼저 보여준다.** 도달을 결정하는 것은 조회가 아니라 답글이므로, 지표 배치가 판단을 유도해야 한다.

---

## 6. 실패 처리

| 실패 | 처리 |
|---|---|
| 권한 없음(`threads_manage_insights` 미보유) | 안내 후 종료. 발행에 영향 없음 |
| 일부 메트릭 미지원 | 해당 항목만 생략하고 나머지 보고 |
| 게시물별 조회 실패 | 그 건만 제외, 건수 표기 |
| API 차단(code=200) | 즉시 중단 + 최우선 경보 |
| 전체 실패 | 리포트 미발송. 경보 1회 |

**인사이트 실패가 발행을 막지 않는다.** 완전히 독립된 워크플로우다.

---

## 7. 신규 설정

### Secrets
추가 없음.

### Variables

| 이름 | 기본값 | 용도 |
|---|---|---|
| `INSIGHTS_ENABLED` | `true` | 스위치 |
| `INSIGHTS_LOOKBACK_DAYS` | `7` | 집계 기간 |
| `INSIGHTS_POST_LIMIT` | `7` | 게시물별 조회 상한 |

### 권한

`threads_manage_insights` 가 필요하다. 이미 보유 중이면 추가 작업이 없고, 없으면 **콘솔 권한 추가 → 토큰 생성기 재발급**으로 해결된다. OAuth 재인가는 불필요하다.

---

## 8. 코드 구성

| 파일 | 역할 |
|---|---|
| `src/insights.py` | 신규. 메트릭 파싱·기둥 복원·집계 |
| `src/run_insights.py` | 신규. 엔트리포인트 |
| `src/threads_client.py` | 인사이트 조회 메서드 추가 |
| `src/config.py` | 위 Variables |
| `.github/workflows/insights.yml` | 신규 |
| `scripts/check_scopes.py` | 인사이트 권한 검사 추가 |
| `scripts/verify_repo.py` | 신규 모듈 등록 |
| `tests/test_insights.py` | 신규 |

---

## 9. 검증 계획

| 그룹 | 항목 |
|---|---|
| 메트릭 파싱 | 시계열 / 총계 / 링크별 총계 3형태 |
| 기둥 복원 | 슬롯 A·B·C, 이벤트, 판정 불가 |
| 집계 | 기둥별 합계·평균, 제외 건수 |
| 실패 처리 | 권한 없음, 부분 실패, 차단 |
| 리포트 | 답글 우선 배치, 판정 불가 표기 |
| 독립성 | 인사이트 실패가 발행에 영향 없음 |

---

## 10. 착수 전 확인

| # | 항목 |
|---|---|
| 1 | `threads_manage_insights` 권한 보유 여부 |
| 2 | 팔로워 100명 미만이면 `follower_demographics` 제외 |
| 3 | 계정 복구 후 안정화 확인 |

2번은 현재 팔로워 수가 100 미만일 것으로 보이므로, 초기에는 이 메트릭을 요청하지 않는다. 요청하면 오류가 나거나 빈 값이 온다.

---

## 11. 이 설계가 바꾸는 것

| 요구사항 | 변경 |
|---|---|
| 미결 U1 (유입 측정) | **해소** — `clicks` 메트릭 |
| 제외 범위 "UTM 기반 추적" | **불필요** — `clicks` 로 대체 |
| 관찰 계획 2단계 기록표 | 수동 기록 → 자동 리포트 |
| 3단계 판정 근거 | 감 → 데이터 |

관찰 2단계의 기둥별 비교가 **수기 집계에서 자동 집계로** 바뀐다. 마스터는 텔레그램 리포트를 읽고 사실성 열만 채우면 된다.

---

# 12. 기둥 비중 자동 조절 (Adaptive Weighting)

## 12-0. 먼저 위험을 명시한다

마스터가 자동 조절을 선호하셨으므로 설계하되, **이 기능은 잘못 만들면 성과를 악화시킨다.** 그 이유를 먼저 적는다.

| 기간 | 총 발행 | 기둥당 | 판단 가능 수준 |
| --- | --- | --- | --- |
| 1주 | 7 | 2.3 | **방향도 불가** |
| 2주 | 14 | 4.7 | **방향도 불가** |
| 1개월 | 30 | 10.0 | 방향 정도 |
| 3개월 | 91 | 30.3 | 비교 가능 |

예상 주간 클릭은 한 자릿수다. **클릭 7 대 2를 "3.5배 우수"로 읽으면 안 된다.** 한 사람이 더 눌렀을 뿐일 수 있다.

표본이 부족한 상태에서 자동 조절을 켜면 다음이 일어난다.

```
1주차  우연히 STORY 클릭 3 -> STORY 비중 상향
2주차  우연히 MARKET 클릭 2 -> MARKET 비중 상향
3주차  다시 STORY -> ...
```

**노이즈를 따라 비중이 진동하고, 어느 기둥도 충분한 표본을 쌓지 못한다.** 자동화가 학습을 방해하는 상태가 된다.

따라서 이 설계의 본질은 "자동으로 바꾸는 것"이 아니라 **"바꿔도 되는 조건을 엄격히 정의하는 것"** 이다.

## 12-1. 안전장치 7종

| # | 장치 | 값 | 막는 것 |
| --- | --- | --- | --- |
| S1 | 최소 표본 | 기둥당 10건 | 노이즈 추종 |
| S2 | 조정 주기 | 30일 1회 | 진동 |
| S3 | 조정 폭 | 1회 ±1칸 (8칸 중) | 급격한 쏠림 |
| S4 | 기둥 하한 | 각 1칸 (12.5%) | 기둥 소멸 |
| S5 | PROMO 상한 | 2칸 (25%) | 홍보 과다로 도달 하락 |
| S6 | 유의 임계 | 1위가 2위의 1.5배 이상 | 근소한 차이에 반응 |
| S7 | 근거 기둥 보호 | STORY 최소 2칸 | 사실성 근거 소실 |

S7의 근거: BUILD 제거 후 STORY만 근거(Notion 트래커)를 갖는다. STORY가 줄면 근거 없는 발행 비율이 올라간다. 성과와 무관하게 지켜야 하는 하한이다.

## 12-2. 점수 산식

광고 채널의 목적은 유입이므로 **클릭이 1순위**다. 다만 클릭 표본이 작아 단독으로는 불안정하므로 답글을 보조 지표로 둔다.

```
score(기둥) = 일평균클릭 × 1.0 + 일평균답글 × 0.3
```

| 지표 | 가중 | 근거 |
| --- | --- | --- |
| 클릭 | 1.0 | 목적 지표. 유입의 직접 증거 |
| 답글 | 0.3 | 도달 선행 지표. 클릭의 상류 |
| 조회 | 0 | **제외.** 읽고 지나가는 것은 광고 성과가 아니다 |
| 좋아요 | 0 | 제외. 행동으로 이어지지 않음 |

조회를 제외한 근거: 파일럿에서 MARKET이 조회 965로 최다였으나 답글은 3으로 최저였다. 조회를 넣으면 **행동을 만들지 않는 기둥이 1위가 된다.**

## 12-3. 조정 알고리즘

```
1. 지난 30일 일별 데이터 수집
2. 날짜 -> 기둥 귀속 (슬롯 역추론)
3. 기둥별 score 계산
4. 게이트 통과 여부 판정
     S1  모든 기둥이 10건 이상인가
     S2  마지막 조정 후 30일 지났는가
     S6  1위 score >= 2위 score × 1.5 인가
   하나라도 미달 -> 조정하지 않고 리포트만
5. 1위 기둥 +1칸, 최하위 기둥 -1칸
6. 불변식 검증 (S4 S5 S7 + 기존 정책 불변식)
     위반 -> 조정 취소, 사유 기록
7. 새 로테이션을 Variable 에 기록
8. 조정 내역 알림
```

## 12-4. 상태 저장 문제

자동 조절은 본질적으로 **상태를 요구한다.** 무상태 원칙(C1)과 충돌한다.

| 방식 | 상태 위치 | 판정 |
| --- | --- | --- |
| 매 발행 시 인사이트 재조회 후 계산 | 없음 | **멱등성 파괴.** 같은 날 재실행이 달라짐 |
| Secret 에 저장 | Secret | 크기·용도 부적합 |
| **GitHub Variable 에 기록** | **Variable** | **채택** |

Variable 2개를 상태로 둔다.

| 이름 | 예시 | 용도 |
| --- | --- | --- |
| `PILLAR_ROTATION_OVERRIDE` | `MARKET,PROMO,MARKET,STORY,MARKET,STORY,MARKET,PROMO` | 현재 로테이션 |
| `LAST_WEIGHT_ADJUST` | `2026-10-20` | 마지막 조정일 (S2 판정용) |

**원칙 위반을 인정한다.** 무상태는 "DB를 두지 않는다"는 뜻이었고, Variable 2개는 그 취지 안에 있다고 본다. 다만 상태가 늘어난 것은 사실이므로 문서에 남긴다.

Variable 쓰기에는 `variables: read and write` 권한을 가진 PAT가 필요하다. 현재 `GH_PAT_SECRETS_WRITE` 는 Secrets 전용이므로 권한 추가 또는 별도 PAT 발급이 필요하다.

## 12-5. 적용 예시 (가정 데이터)

30일 후 이런 결과가 나왔다고 하자.

| 기둥 | 발행 | 클릭 | 답글 | 일평균클릭 | 일평균답글 | score |
| --- | --- | --- | --- | --- | --- | --- |
| STORY | 10 | 18 | 24 | 1.80 | 2.40 | **2.52** |
| MARKET | 12 | 6 | 9 | 0.50 | 0.75 | 0.73 |
| PROMO | 8 | 9 | 3 | 1.13 | 0.38 | 1.24 |

게이트 판정

| 게이트 | 결과 |
| --- | --- |
| S1 표본 10건 이상 | 통과 (10 / 12 / 8) → **PROMO 8건 미달로 보류** |

PROMO가 8건이므로 **조정하지 않는다.** 리포트만 발송하고 다음 주기를 기다린다.

S1을 통과했다고 가정하면 다음과 같다.

```
1위 STORY 2.52, 2위 PROMO 1.24 -> 2.03배 >= 1.5 (S6 통과)
STORY +1칸, MARKET -1칸

변경 전  MARKET,PROMO,MARKET,STORY,MARKET,PROMO,MARKET,STORY  (M4 P2 S2)
변경 후  MARKET,PROMO,MARKET,STORY,MARKET,STORY,MARKET,PROMO  (M3 P2 S3)

불변식 검증
  S4 각 기둥 1칸 이상       통과
  S5 PROMO 2칸 이하        통과
  S7 STORY 2칸 이상        통과
  연속 중복 없음            통과
```

## 12-6. 수동 우선

| Variable | 기본값 | 동작 |
| --- | --- | --- |
| `ADAPTIVE_WEIGHTS_ENABLED` | `false` | 자동 조절 스위치 |
| `PILLAR_ROTATION_OVERRIDE` | 미설정 | 수동 지정 시 자동 조절 무시 |

**기본값은 비활성이다.** 최소 1개월 데이터가 쌓이기 전에는 켜지 않는다. 마스터가 수동으로 지정하면 자동 조절보다 우선한다.

## 12-7. 조정 알림

```
[Threads 비중 조정] 2026-10-20

30일 집계 (기둥당 최소 10건 충족)
  STORY   발행10  클릭18  답글24  score 2.52
  PROMO   발행 8  클릭 9  답글 3  score 1.24
  MARKET  발행12  클릭 6  답글 9  score 0.73

판정: STORY 가 2위의 2.03배 (임계 1.5배 초과)

조정
  STORY  2칸 -> 3칸
  MARKET 4칸 -> 3칸

다음 조정 가능일: 2026-11-19
```

조정하지 않은 경우도 사유와 함께 통지한다.

```
[Threads 비중 조정] 2026-10-20
조정 보류 — PROMO 표본 8건 (최소 10건 미달)
다음 판정: 2026-10-27
```

## 12-8. 신규 구성

| 파일 | 역할 |
| --- | --- |
| `src/weighting.py` | 점수 계산·게이트·조정안 생성 |
| `src/run_weighting.py` | 엔트리포인트 |
| `.github/workflows/weighting.yml` | 매주 일요일 판정 (조정은 30일 1회) |
| `src/config.py` | `ADAPTIVE_WEIGHTS_ENABLED` 외 |
| `src/ai_writer.py` | `PILLAR_ROTATION_OVERRIDE` 반영 |

## 12-9. 착수 조건

| 조건 | 기준 |
| --- | --- |
| 인사이트 수집 가동 | 선행 필수 |
| 데이터 축적 | **최소 30일** |
| 기둥당 표본 | 10건 이상 |
| Variable 쓰기 PAT | 권한 추가 |
| 계정 안정 | checkpoint 재발 없음 |

**인사이트 없이 자동 조절은 불가능하다.** 구현 순서는 인사이트 → 30일 관찰 → 자동 조절이다.

## 12-10. 판단 — 이 기능을 언제 켜야 하나

| 시점 | 권고 |
| --- | --- |
| 지금 | **구현하되 비활성** |
| 30일 후 | 리포트로 수동 판단 1회 |
| 60일 후 | 수동 판단과 자동 산출이 일치하면 활성화 |

60일 시점에 **"자동 산출이 사람 판단과 같은 결론을 내는지"** 를 먼저 확인한다. 다르면 산식이 잘못된 것이므로 켜지 않는다.
