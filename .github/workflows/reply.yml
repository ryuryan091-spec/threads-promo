name: Threads Reply

# 내 글에 달린 댓글에 자동 답글.
# v1.1.0: 3개 슬롯 모두 실행한다(1-of-3 추첨 제거). 오전(09~12시)은 chat.yml 이
# 실행마다 답글 스윕을 수행하므로, 이 워크플로우는 오후·저녁을 담당한다.
# 일일 캡(20)·저자 캡(2)은 실행 수와 무관하게 Threads 조회로 하루 단위 유지된다.

"on":
  schedule:
    # KST 12:19 / 16:53 / 22:07
    #   발행 슬롯(08:23/12:47/20:31)·CHAT 창(09:00~12:05)과 겹치지 않게 배치.
    - cron: "19 3 * * *"
    - cron: "53 7 * * *"
    - cron: "07 13 * * *"
  workflow_dispatch:
    inputs:
      mode:
        description: "실행 모드"
        type: choice
        options: [dry_run, live]
        default: dry_run

permissions:
  contents: read

concurrency:
  group: threads-reply
  cancel-in-progress: false

jobs:
  reply:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          # BUILD 기둥 제거로 커밋 로그는 현재 미사용.
          # 기둥을 되살릴 때를 대비해 depth 는 유지한다(비용 없음).
          fetch-depth: 50

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - run: pip install -r requirements.txt

      - name: Resolve slot
        id: slot
        run: |
          case "${{ github.event.schedule }}" in
            "19 3 * * *")  echo "slot=A" >> "$GITHUB_OUTPUT" ;;
            "53 7 * * *")  echo "slot=B" >> "$GITHUB_OUTPUT" ;;
            "07 13 * * *") echo "slot=C" >> "$GITHUB_OUTPUT" ;;
            *)             echo "slot=MANUAL" >> "$GITHUB_OUTPUT" ;;
          esac

      - name: Reply
        env:
          THREADS_APP_ID: ${{ secrets.THREADS_APP_ID }}
          THREADS_APP_SECRET: ${{ secrets.THREADS_APP_SECRET }}
          THREADS_USER_ID: ${{ secrets.THREADS_USER_ID }}
          THREADS_LONG_LIVED_TOKEN: ${{ secrets.THREADS_LONG_LIVED_TOKEN }}
          GH_PAT_SECRETS_WRITE: ${{ secrets.GH_PAT_SECRETS_WRITE }}
          TOKEN_ISSUED_AT: ${{ vars.TOKEN_ISSUED_AT }}
          TOKEN_REFRESHED_AT: ${{ secrets.TOKEN_REFRESHED_AT }}
          CLAUDE_AI_KEY: ${{ secrets.CLAUDE_AI_KEY }}
          CLAUDE_MODEL: ${{ vars.CLAUDE_MODEL }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_ALERT_CHAT_ID: ${{ secrets.TELEGRAM_ALERT_CHAT_ID }}
          YOUTUBE_URL: ${{ vars.YOUTUBE_URL }}
          X_URL: ${{ vars.X_URL }}
          REPLY_ENABLED: ${{ vars.REPLY_ENABLED || 'true' }}
          EVENT_NAME: ${{ github.event_name }}
          REPLY_SLOTS: "A,B,C"
          SLOT: ${{ steps.slot.outputs.slot }}
          DRY_RUN: ${{ github.event_name == 'workflow_dispatch' && inputs.mode == 'live' && 'false' || vars.DRY_RUN || 'true' }}
        run: python -m src.run_reply
