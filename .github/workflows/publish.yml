name: Threads Publish

"on":
  schedule:
    # KST 09:30 = UTC 00:30
    # KST 08:23(출근) / 12:47(점심) / 20:31(저녁)
    #   - 최적 시간대 3개에 하나씩 배치. 하루 하나만 실제 실행.
    #   - 정각·반각 회피. 실제 발행 범위 12시간 이상으로 분산.
    - cron: "23 23 * * *"
    - cron: "47 3 * * *"
    - cron: "31 11 * * *"
  workflow_dispatch:
    inputs:
      mode:
        description: "실행 모드"
        type: choice
        options: [dry_run, live]
        default: dry_run

permissions:
  contents: read

concurrency:
  group: threads-publish
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          # BUILD 기둥 제거로 커밋 로그는 현재 미사용.
          # 기둥을 되살릴 때를 대비해 depth 는 유지한다(비용 없음).
          fetch-depth: 50

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - run: pip install -r requirements.txt

      - name: Resolve slot
        id: slot
        run: |
          case "${{ github.event.schedule }}" in
            "23 23 * * *") echo "slot=A" >> "$GITHUB_OUTPUT" ;;
            "47 3 * * *")  echo "slot=B" >> "$GITHUB_OUTPUT" ;;
            "31 11 * * *") echo "slot=C" >> "$GITHUB_OUTPUT" ;;
            *)             echo "slot=MANUAL" >> "$GITHUB_OUTPUT" ;;
          esac

      - name: Publish
        env:
          THREADS_APP_ID: ${{ secrets.THREADS_APP_ID }}
          THREADS_APP_SECRET: ${{ secrets.THREADS_APP_SECRET }}
          THREADS_USER_ID: ${{ secrets.THREADS_USER_ID }}
          THREADS_LONG_LIVED_TOKEN: ${{ secrets.THREADS_LONG_LIVED_TOKEN }}
          GH_PAT_SECRETS_WRITE: ${{ secrets.GH_PAT_SECRETS_WRITE }}
          TOKEN_ISSUED_AT: ${{ vars.TOKEN_ISSUED_AT }}
          CLAUDE_AI_KEY: ${{ secrets.CLAUDE_AI_KEY }}
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DB_ID: ${{ secrets.NOTION_DB_ID }}
          NOTION_FIELD_ALLOWLIST: ${{ vars.NOTION_FIELD_ALLOWLIST }}
          CLAUDE_MODEL: ${{ vars.CLAUDE_MODEL }}
          AI_ENABLED: ${{ vars.AI_ENABLED || 'true' }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_ALERT_CHAT_ID: ${{ secrets.TELEGRAM_ALERT_CHAT_ID }}
          YOUTUBE_URL: ${{ vars.YOUTUBE_URL }}
          X_URL: ${{ vars.X_URL }}
          ASSET_RAW_BASE_URL: ${{ vars.ASSET_RAW_BASE_URL }}
          EVENT_NAME: ${{ github.event_name }}
          PUBLISH_SLOTS: "A,B,C"
          PUBLISH_WEEKLY_REST_DAYS: ${{ vars.PUBLISH_WEEKLY_REST_DAYS || '0' }}
          SLOT: ${{ steps.slot.outputs.slot }}
          DRY_RUN: ${{ github.event_name == 'workflow_dispatch'
             && inputs.mode == 'live' && 'false'
             || vars.DRY_RUN || 'true' }}
        run: python -m src.main
